[project]
name = "ui"
version = "0.1.0"
description = "Streamlit UI for Multi-Agent RAG"
requires-python = ">=3.11"
dependencies = [
  "streamlit==1.39.0",
  "httpx==0.27.2"
]

[tool.uv]
python-preference = "managed"
